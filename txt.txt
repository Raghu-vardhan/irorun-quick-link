    {% assign collection = sections.settings.select-collection %}

    {% if collection %}
    {% for product in collection.products %}    
            <div class="product-grid"> 
                <a href="{{product.url}}">
                <div class="product-card">
                        <img src="{{product.featured_image | image_url: 'medium'}}" alt="{{product.title}} height="400" widht="400" />
                </div>     

                <div class="product-details">
                        <h2>{{product.title}}</h2>
                        <p>{{product.price | money}}</p>
                </div>
                </a> 
            </div>
        {% forend %}

        {% if product.available %}
            <form method="post" action="/cart/add">
                <input type="hidden" name="id" value="{% assign collection = {{sections.settings.select-collection}} %}
                <button type="button" value="Add to cart">Add to cart</button>
            </form>
        {% endif %}   
    {% else %}
        <p>Not selected </p>
    {% endif %}



    {% schema %}
        {

            "name":"collection custom",
            "settings":[
                {
                    "type":"collection",
                    "id":"selected-collection",
                    "label":"select collection"
                }
            ],
            "presets":[
                {
                    "name":"collection custom",
                    "category":"custom"
                }
            ]
        }
    {% endschema %}